Index: configure.ac
===================================================================
--- configure.ac	(revision 20823)
+++ configure.ac	(working copy)
@@ -1071,6 +1070,7 @@
       CFLAGS_OPTIM_SPEED="${CFLAGS_OPTIM_SPEED} -funroll-loops"
   fi
 fi
+CFLAGS="${CFLAGS_save}"
 
 dnl Check for -fomit-frame-pointer
 AC_CACHE_CHECK([if \$CC accepts -fomit-frame-pointer],
