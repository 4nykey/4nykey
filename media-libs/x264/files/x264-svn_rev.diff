Index: version.sh
===================================================================
--- version.sh	(revision 622)
+++ version.sh	(working copy)
@@ -1,5 +1,5 @@
 #!/bin/sh
-VER=`svnversion .`
+VER=${ESVN_WC_REVISION}
 if [ "x$VER" != x -a "$VER" != exported ]
 then
   echo "#define X264_VERSION \" svn-$VER\"" >> config.h
