--- a/src/x11-force-focus.c
+++ b/src/x11-force-focus.c
@@ -43,7 +43,7 @@ gdk_x11_window_force_focus (GdkWindow *w
 
     display = GDK_WINDOW_DISPLAY (window);
 
-    if (GTK_CHECK_VERSION(3,0,0) && !GDK_IS_X11_DISPLAY (display))
+    if (GTK_CHECK_VERSION(3,0,0) && !GDK_IS_DISPLAY (display))
         return;
 
     if (gdk_x11_screen_supports_net_wm_hint (gdk_window_get_screen (window),
