Index: generate_version_cc.sh
===================================================================
--- generate_version_cc.sh	(revision 624)
+++ generate_version_cc.sh	(working copy)
@@ -5,7 +5,11 @@
     exit
 fi
 
+if [[ -d .svn ]]; then
 export VERSION_STAMP="`svnversion -n` `date +%Y-%m-%d` `uname -o -i`"
+else
+VERSION_STAMP="${ESVN_WC_REVISION} `date +%Y-%m-%d` `uname -o -i`"
+fi
 
 cat << EOF > $1
 // Generated by generate_version_cc.sh
