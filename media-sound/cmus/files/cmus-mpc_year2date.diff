--- mpc.c.orig	2006-12-22 00:32:39.000000000 +0200
+++ mpc.c	2006-12-22 00:33:42.000000000 +0200
@@ -263,6 +263,9 @@
 			 */
 			free(key);
 			key = xstrdup("discnumber");
+		} else if (!strcasecmp(key, "year")) {
+			free(key);
+			key = xstrdup("date");
 		}
 
 		if (!is_interesting_key(key)) {
