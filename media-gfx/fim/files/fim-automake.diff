Index: configure.ac
===================================================================
--- configure.ac	(revision 534)
+++ configure.ac	(working copy)
@@ -93,7 +93,6 @@
 AC_MSG_RESULT($fim_cv_prefix)
 
 AC_ISC_POSIX
-AM_C_PROTOTYPES
 if test "x$U" != "x"; then
   AC_MSG_ERROR(Compiler not ANSI compliant)
 fi
